[template]
src = "nubis-users.tmpl"
dest = "/etc/puppet/yaml/nubis_users/common.yaml"
prefix = "/environments/%%ENVIRONMENT%%/global/nubis-users"

keys = [
<% if @global_admins -%><% @global_admins.each do |global_users| -%>
    "/<%= global_users %>",
<% end -%><% end -%>
<% if @sudo_users -%><% @sudo_users.each do |sudo_users| -%>
    "/<%= sudo_users %>",
<% end -%><% end -%>
<% if @nubis_users -%><% @nubis_users.each do |nubis_users| -%>
    "/<%= nubis_users %>",
<% end -%><% end -%>
]

reload_cmd = "puppet apply --hiera_config /etc/puppet/hiera.yaml --modulepath /etc/puppet/modules/ -e 'include nubis_users'"
